# 01 · 架构设计总览

## 1. 目标与非目标

### 1.1 本期目标

| 编号 | 目标 | 验收方式 |
|---|---|---|
| G1 | 建立三层关键词（场景 / 用户 / Agent）的统一权重模型 | 纯函数单测，无需联网即可验证 |
| G2 | 对话过程中自动分析并增量更新用户肖像 | 脚本化剧本回放，断言权重演化方向 |
| G3 | 由 L1 × L2 推导出当轮 Agent 人格并组装 System Prompt | 给定输入可复现同一 Prompt |
| G4 | 人格切换平滑，不出现"人设跳变" | 滞回阈值 + 冷却轮次，有审计记录 |
| G5 | 通过 CLI 完成完整对话闭环，且能解释"为什么这么回" | `dla chat --explain` 打印三层权重 |
| G6 | PyQt 界面可实时对话，并可视化三层权重演化 | 手动冒烟 + 长对话不卡顿 |

### 1.1.1 对话与界面语言约定

- **默认中文，但中英混合是自然允许的**。用户可以中文夹杂英文术语（如"这个 token 有点问题"），Agent 也应在保持中文主体的同时自然保留必要的英文专业词，而不是强行翻译或拒绝。
- 语言**靠场景模板与约束维度引导，不靠硬编码 if 判断**。例如语言学习场景用 `constraint.english_only` 维度去促成英文沉浸，日常场景则放松。框架层不写"如果语言==中文则…"。
- 词库、UI 文案、给 LLM 的判定说明（keyword 的 `description`）一律中文，方便后续开源社区贡献。

### 1.2 明确不做（避免范围蔓延）

- 不做多用户账号体系 / 权限 / 云端同步。首期为**单机单用户**，但数据模型预留 `user_id` 字段。
- 不做**对话历史**的向量检索 / RAG / 语义搜索——**但此条已被 doc/07 推翻并单独实现**：对话历史的冷热记忆（Hot/Cold Memory）由 **LangGraph + 本地向量库**以独立子系统实现（见 [07 · 对话历史冷热记忆](./07-对话历史冷热记忆(LangGraph+RAG).md)）。该子系统的检索对象是"对话片段/抽取事实"，与本项目的"词库向量化"（doc/06，对象是关键词、用途是关联学习）是**两个独立向量空间**，互不替代。冷记忆只作 Prompt 注入、**不参与权重计算**。
- 不做**大模型微调**。人格调节全部通过 Prompt 实现；被训练的只有一个很小的**本地关联模型**（d×d 双线性矩阵，纯 numpy），而非 LLM 权重。
- 不做语音输入输出，**但在 `core/ports.py` 中预留 `SpeechPort` 接口抽象**（含 `transcribe` 语音识别 / `synthesize` 语音合成两个方向），默认提供 **no-op 实现**。**未来接入 Whisper（ASR）/ TTS 引擎即插即用**——核心引擎与 UI 不感知具体语音后端，只调用端口；是否启用由配置开关 `DLA_SPEECH__ENABLED` 控制，本期**不实现语音链路本身**。
- 不引入消息队列与独立任务进程。后台分析在进程内的受控异步中完成即可，任务量远未到需要 Celery 的程度。

## 2. 术语表

| 术语 | 含义 |
|---|---|
| **关键词（Keyword）** | 一个带语义的原子标签，如 `mood.frustrated`、`tone.encouraging`。分**类别型**与**程度型**两种。 |
| **类别型 / categorical** | 互斥选项，取且仅取一个，如 `role` ∈ {外教, 同事, 朋友}。按权重取 argmax。 |
| **程度型 / scalar** | 连续值，归一化到 `[0,1]`，如 `empathy=0.8`、`verbosity=0.3`。按权重加权平均。 |
| **维度（Dimension）** | 同类关键词归为一个维度，维度内做归一化与冲突消解。 |
| **权重（Weight）** | 该关键词对最终 Prompt 的影响力，见 [02 文档](./02-三层关键词权重模型.md)。 |
| **置信度（Confidence）** | 我们对这条判断有多大把握，与"权重"是两个独立概念。 |
| **证据（Evidence）** | 支撑某条用户肖像判断的原始观测，带强度与时间戳，是置信度的来源。 |
| **快照（Snapshot）** | 某一轮次三层权重的完整存档，用于可视化与离线调参。 |

## 3. 技术选型

| 层面 | 选择 | 理由 |
|---|---|---|
| 语言 | Python 3.13 | 与 LLM/数据生态一致，团队无跨语言成本 |
| LLM 接入 | `openai` SDK + 可配 `base_url` | 一套代码适配 OpenAI / DeepSeek / 通义 / 本地网关，切换只改环境变量 |
| 结构化输出 | JSON Schema 约束 + 白名单校验 | 关键词必须是受控词表内的，绝不允许模型自由发挥造词 |
| 存储 | SQLite（标准库 `sqlite3`） | 零运维、单文件、本地场景够用；仓储层抽象后可换 PostgreSQL |
| 迁移 | 自建 migration runner | Schema 简单，引入 Alembic 收益不足以抵消依赖成本 |
| 配置 | `pydantic-settings` + `.env` | 启动时集中校验、快速失败 |
| CLI | `typer` + `rich` | 声明式参数，表格/树形输出开箱即用 |
| 桌面 UI | **PyQt6**（经 `qt_compat` 抽象） | 见下方许可证说明 |
| 记忆编排 | **LangGraph**（`langgraph` + `SqliteSaver`） | 对话历史冷热记忆（doc/07）的有状态编排与 checkpoint；纯 Python、离线可用 |
| 记忆嵌入 | `sentence-transformers`（可选） | 冷记忆语义检索的 embedding backbone；留空则用确定性占位，结构可测但语义退化 |
| 工具框架 | 自研插件系统（零额外强依赖；可选 `watchdog` 做文件监听热更新） | Agent 本地工具以插件单元组织，支持运行时热更新与两级工具路由（doc/08） |
| 日志 | `structlog` 风格 JSON 输出 | 便于后续排查与离线分析 |
| 测试 | `pytest` + `pytest-cov` | 权重引擎为纯函数，单测覆盖率目标 ≥ 85% |
| 质量 | `ruff` + `mypy` | 核心引擎强制类型标注 |

### 3.1 关于 Qt 绑定与许可证

**已确认本项目未来必定开源。** 这直接消解了之前最大的许可证风险：PyQt6 采用 **GPLv3 / 商业双许可**，而 GPLv3 与开源分发完全兼容——只要按 GPLv3 发布（保留版权与许可声明、提供源码），使用 PyQt6 没有任何合规障碍，无需购买商业许可，也无需切到 PySide6。

**决策**：默认使用 PyQt6（GPLv3），按开源协议发布。尽管如此，所有 Qt 导入仍经过 `src/dla/ui/qt/qt_compat.py` 这一层：

```python
# qt_compat.py —— 切换绑定只需改这一个文件
try:
    from PyQt6.QtCore import QObject, QThread, pyqtSignal as Signal
    from PyQt6.QtWidgets import QApplication, QMainWindow
except ImportError:
    from PySide6.QtCore import QObject, QThread, Signal
    from PySide6.QtWidgets import QApplication, QMainWindow
```

这层抽象的成本接近零，但保留了将来切换绑定（例如出于打包体积或上游支持考虑）的灵活性。

## 4. 分层架构

整体遵循**按功能组织 + 三层架构**，核心原则：**控制器不写业务逻辑，核心引擎不依赖任何 UI 框架**。

```
┌─────────────────────────────────────────────────────────────┐
│  交互层 (Adapters)        可替换，互不依赖                    │
│  ┌──────────┐   ┌──────────────┐   ┌──────────────────┐    │
│  │ CLI      │   │ FastAPI      │   │ PyQt6 桌面界面    │    │
│  │ (typer)  │   │ (可选启用)    │   │ (首期主界面)      │    │
│  └────┬─────┘   └──────┬───────┘   └────────┬─────────┘    │
└───────┼────────────────┼────────────────────┼──────────────┘
        │                │                    │
        └────────────────┴────────────────────┘
                         │  统一调用接口（同步 + 异步双入口）
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  编排层 (Orchestration)                                      │
│  DialogueEngine —— 一轮对话的完整流水线，唯一的流程所有者      │
└────────────────────────┬────────────────────────────────────┘
                         │
     ┌───────────────────┼───────────────────┬──────────────┐
     ▼                   ▼                   ▼              ▼
┌─────────┐      ┌─────────────┐     ┌────────────┐  ┌──────────┐
│ Analysis│      │  Weighting  │     │   Prompt   │  │  Storage │
│ 分析器   │─────▶│  权重引擎    │────▶│  组装器     │  │  仓储层   │
│ 抽取证据 │      │ 三层计算     │     │ 预算+渲染   │  │ SQLite   │
└─────────┘      └──────┬──────┘     └────────────┘  └──────────┘
     │                   │                   │
     │        ┌──────────┴───────────┐       │
     │        │  Lexicon LVM          │       │
     │        │  向量化词库 + 在线学习 │◀──────┤ 回写关联矩阵 / 读取先验
     │        │  embedding + M_r 训练 │       │
     │        └──────────┬───────────┘       │
     │                   │ 提供 L3 学习先验 γ  │
     └───────────────────┼───────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  外部依赖 (Ports)                                            │
│  LLMClient（OpenAI 兼容） / Clock / Repository / Config / SpeechPort(预留,no-op)      │
└─────────────────────────────────────────────────────────────┘
```

**记忆增强层（doc/07，独立子系统）**：对话历史的冷热记忆由 **LangGraph** 以"外层编排"方式承载——它在 `DialogueEngine` 生成之前插入 `retrieve_cold`（冷记忆检索）、在生成之后插入 `reflect_store`（入库与热→冷迁移），并通过 `SqliteSaver` 把图状态 checkpoint 到同一 SQLite。该层**不改动**核心引擎与权重计算，冷记忆只作 Prompt 注入。详见 [07 · 对话历史冷热记忆](./07-对话历史冷热记忆(LangGraph+RAG).md)。

**工具插件与路由层（doc/08，编排层内部子系统）**：Agent 可调用的本地工具（含 `recall_memory`）以**插件单元**组织，统一 `Tool` 契约，由 `ToolRegistry` 以原子快照管理，支持运行时热更新（不中断进行中对话）；当 LLM 需调用工具时由**两级路由**（低成本路由粗筛 top-N 候选 → LLM 在候选中选定）减负，避免全量 schema 注入。工具结果只进当前轮上下文、**不参与权重计算**。详见 [08 · 工具插件化与路由](./08-工具插件化与路由.md)。

**关键约束**：`src/dla/` 下的核心包**不允许 import 任何 Qt、typer、fastapi 模块**。这条约束用一条 CI 级检查（`ruff` 自定义规则或测试断言）来守住，违例即失败。记忆层（LangGraph 图）同样视为"编排/适配"范畴，核心权重引擎与 LVM 不反向依赖它。

## 5. 目录结构

```
DialogueLearningAgent/
├── doc/                              # 本套设计方案
├── config/
│   ├── keywords/                     # 三层关键词库（YAML，可版本化管理）
│   │   ├── l1_scenario.yaml
│   │   ├── l2_user_portrait.yaml
│   │   └── l3_agent_persona.yaml
│   ├── coupling_rules.yaml           # 层间耦合规则（L1×L2 → L3）
│   ├── scenarios/                    # 场景预设模板
│   │   ├── oral_practice.yaml
│   │   └── interview_mock.yaml
│   └── prompt_templates.yaml         # Prompt 骨架与渲染模板
├── migrations/
│   ├── 001_init.sql
│   └── 002_add_persona_changes.sql
├── src/dla/
│   ├── core/                         # 领域模型 + 端口抽象（无外部依赖）
│   │   ├── models.py                 # Keyword / Evidence / Layer / Snapshot
│   │   ├── ports.py                  # LLMClient / Repository / Clock / SpeechPort(预留,no-op) 协议
│   │   └── errors.py                 # 类型化错误体系
│   ├── config/
│   │   ├── settings.py               # pydantic-settings，启动时校验
│   │   └── loader.py                 # YAML 词库加载 + schema 校验
│   ├── keywords/
│   │   ├── lexicon.py                # 词库索引、白名单校验、同义词归一
│   │   ├── vectorizer.py             # 关键词 embedding、关联矩阵 M_r、上下文检索（见 06）
│   │   └── learning.py               # 在线学习：损失/梯度/更新、回放缓冲、检查点（见 06）
│   ├── evolution.py              # 词库自动演化：候选发现 / LLM 判定 / 升级审批
│   │   └── registry.py               # 三层注册表
│   ├── weighting/
│   │   ├── confidence.py             # 证据 → 置信度
│   │   ├── decay.py                  # 时间衰减
│   │   ├── coupling.py               # L1×L2 → L3 规则推导
│   │   ├── resolver.py               # 维度内归一化与冲突消解
│   │   └── engine.py                 # 三层权重计算总入口
│   ├── analysis/
│   │   ├── heuristics.py             # 本地启发式（零成本、即时）
│   │   ├── llm_analyzer.py           # LLM 抽取（带校验与重试）
│   │   └── trigger.py                # 触发策略（控制调用成本）
│   ├── tools/                        # doc/08：工具插件化与路由
│   │   ├── protocol.py               # Tool 协议 / ToolContext / ToolResult
│   │   ├── registry.py               # ToolRegistry：原子快照 + 热更新 + 回滚
│   │   ├── loader.py                 # 插件扫描 / entry_points 加载 / 静态校验
│   │   ├── router.py                 # 两级路由：规则/embedding 粗筛 → LLM 选定
│   │   ├── executor.py               # 工具派发执行 + 超时熔断 + 降级
│   │   └── plugins/                  # 内置与第三方插件（含 recall_memory）
│   │       └── recall_memory/        # doc/07 §4.7 工具，作为首个内置插件示例
│   ├── prompt/
│   │   ├── budget.py                 # Token 预算分配与裁剪
│   │   ├── renderer.py               # 关键词 → 自然语言片段
│   │   └── assembler.py              # 三段组装 + 稳定性判定
│   ├── orchestration/
│   │   └── engine.py                 # DialogueEngine：一轮完整流水线
│   ├── memory/                         # doc/07：对话历史冷热记忆（LangGraph + RAG）
│   │   ├── graph.py                  # LangGraph StateGraph：retrieve/prepare/generate/store/compact
│   │   ├── retriever.py             # 冷记忆语义检索（top-K + rerank）
│   │   ├── store.py                 # 冷库写入 / 热→冷迁移 / 压缩
│   │   ├── embedder.py              # 记忆 embedding（backbone 可选 / 占位回退）
│   │   └── state.py                 # HotMemory / ColdMemoryItem / State schema
│   ├── storage/
│   │   ├── sqlite.py                 # 连接与连接池策略
│   │   ├── repositories.py           # 仓储实现
│   │   └── migrator.py               # 版本化迁移执行器
│   ├── llm/
│   │   └── openai_compat.py          # OpenAI 兼容客户端 + 流式 + 重试
│   └── logging_setup.py              # 结构化 JSON 日志
├── apps/
│   ├── cli/                          # typer 入口
│   ├── api/                          # FastAPI（可选启用）
│   └── qt/                           # PyQt6 界面
│       ├── qt_compat.py
│       ├── main_window.py
│       ├── weight_panel.py           # 三层权重可视化
│       └── workers.py                # QThread 流式 worker
├── tests/
│   ├── unit/                         # 权重引擎纯函数测试
│   ├── integration/                  # SQLite 真实库 + 假 LLM
│   └── fixtures/scripts/             # 对话剧本（回归测试用）
├── .env.example
├── pyproject.toml
└── README.md
```

## 6. 关键设计决策

每条一句话说明取舍：

| # | 决策 | 理由 |
|---|---|---|
| D1 | 关键词分**类别型 / 程度型**两类 | 纯互斥标签会导致硬冲突（"简洁"vs"详尽"）；程度型用连续值加权平均，天然可共存 |
| D2 | L2 的预设项与观察项**共用同一套证据公式** | 预设项反推一个等效初始证据强度即可统一，代码路径唯一，避免两套逻辑漂移 |
| D3 | L3 **不直接接受用户输入**，只由 L1×L2 推导 | 保证人格始终服务于场景与用户，而不是随机设定 |
| D4 | 耦合规则默认走**确定性规则**，LLM 精炼可选且融合权重可配 | 规则可解释、零延迟、零成本；LLM 兜住规则覆盖不到的长尾，β 默认 0.3 |
| D5 | Prompt 采用**分层 Token 预算 + 按权重降序裁剪** | 防止长对话后关键词无限膨胀挤爆上下文 |
| D6 | 人格重建引入**滞回阈值 + 冷却轮次** | 避免用户一句话就让人格翻天覆地，产生"精神病"观感 |
| D7 | 分析器**不是每轮都调 LLM** | 成本与延迟控制；冷启动强制 + 周期 + 启发式事件触发三者结合 |
| D8 | 核心引擎提供**同步 / 异步双入口** | Qt 侧用同步 + QThread（最稳，避免与 asyncio 事件循环打架），CLI 与 API 用异步流式 |
| D9 | 存储用标准库 `sqlite3` + 自建迁移 | 依赖最小化；Repository 抽象保留替换空间 |
| D10 | 每轮保存**权重快照与 L3 变更审计** | 这是本项目的可观测性核心，也是后续调参的数据基础 |
| D11 | 关键词必须命中**白名单词表** | 模型自由造词会导致词库爆炸、权重稀释，一律丢弃非法词并记日志 |
| D12 | 分析失败**静默降级**，绝不中断对话 | 分析是增强项不是必需项；失败时沿用上一轮权重 |
| D13 | **词库随对话自动演化**，新增/调整词的判定交由 LLM | 用户/领域语言不可枚举，封闭词表会随时间僵化；LLM 负责"该不该收编这个新概念"的判定，人工审核兜底 |
| D14 | **默认中文、中英混合**；语言靠场景模板引导而非硬编码判断 | 真实对话天然夹杂术语；用 constraint 维度引导语言，不写死 if 语言==中文 |
| D15 | **调试模式输出完整思维链**（触发→抽取→规则→Δ→组装） | 可解释性是本项目内核，调试器要把"为什么这么回"拆解到人能逐帧看懂 |
| D16 | **词库向量化 + 在线学习**：关键词编码为 d 维向量，跨层关联用可训练双线性矩阵 `M_r` 表示；`M_r` 按学习率 + 损失函数（软交叉熵 + 排序边界 + 权重衰减）用解析梯度更新 | 关联可学习、可计算，且完全离线、纯 numpy、无 GPU；LLM 不微调，只提供监督信号 |
| D17 | **LLM 实时增删改词库**：每轮分析额外产出 `agent_ops`（add/update/delete），作用于当前场景工作集，带护栏（基础词不可删、add 走审核、delta 有界） | 词库随对话自适应，但不失控；所有操作可审计可回放 |
| D18 | **双反馈回路**：前向（用户肖像丰富 → Agent 关键词贴合）+ 后向（满意度 → 反向调整 Agent 权重），共用同一 `M_r` | 最终效果：Agent 人格从场景模板默认值收敛到"这个用户、这种风格最满意"的定点 |
| D19 | **双运行模式**：固定场景模式（用户显式指定场景模板，L1 关键词固定不自动改）与自动场景模式（无预设，LLM 依对话实时判定并更新 L1 场景关键词 `scene_ops`） | 固定模式可控、可复现，适合已知/教学场景；自动模式零配置上手、覆盖未知场景，代价是 L1 在前几轮不确定 |
| D20 | **开场称呼由模型依场景权重生成，无权重时回退默认** | 称呼是人格的第一印象，应与场景一致；冷启动（自动模式尚未建立 L1）用"你好 / hi"兜底，避免空洞或错配 |
| D21 | **对话历史冷热记忆以 LangGraph 单独实现（doc/07）**：热记忆=当前会话滑动窗口+活跃关键词状态（in-graph state，零检索）；冷记忆=历史轮次/抽取事实（向量库 top-K 检索按需注入）；LangGraph 图带 checkpoint 持久化 | 长期记忆与关键词关联是两件事——doc/06 向量化"词"、doc/07 向量化"对话"；冷记忆**只作 Prompt 注入、不参与权重**，人格仍由 L1×L2 决定 |
| D22 | **对话历史以"压缩摘要链"为默认载体，详情按需拉取**：LLM 每轮回复必须在 schema 回传 `turn_summary`（≤100 字）；下一轮注入的历史**仅**由历轮 `turn_summary` 拼接，完整对话原文不进默认 Prompt；需要细节时由本地 `recall_memory` 工具按需取回原文片段再喂回 | 默认历史用摘要可把 token 占用从"N 轮全文"压到"N×100 字"；原文保留在冷热记忆供按需检索，既不丢上下文也不撑爆预算；工具结果只进当前轮、不污染长期权重 |
| D23 | **语音输入输出本期不做，但预留 `SpeechPort` 端口抽象**（ASR `transcribe` / TTS `synthesize` 两个方向，默认 no-op） | 语音是文本对话内核之外的独立通道（ASR 前置 / TTS 后置）；先留接口、不在本期实现，避免日后接 Whisper/TTS 时改动核心引擎与 UI；是否启用由配置开关 `DLA_SPEECH__ENABLED` 控制 |
| D24 | **本地工具以插件单元组织，支持运行时热更新（doc/08）**：统一 `Tool` 契约 + `ToolRegistry` 原子快照；进行中对话持旧快照、新对话用新快照；加载失败自动回滚 | 工具能力会持续扩展，硬编码进引擎会腐烂；热更新让"加功能"不必重启进程、不中断进行中对话 |
| D25 | **LLM 调用本地工具走两级路由**：低成本路由先粗筛 top-N 候选（规则/embedding），再让 LLM 在候选中选定 | 全量工具 schema 注入会 token 膨胀且降低选择质量；路由把"该调哪个"的粗活从 LLM 身上卸下，LLM 只做最终裁决 |

## 7. 一轮对话的完整数据流

```
用户消息进入
   │
   ├─ 0. 会话开始（仅新会话）
   │      ├─ 读取运行模式 DLA_MODE__SCENARIO（fixed | auto）
   │      ├─ fixed 模式：加载用户指定/选择的场景模板 → L1 权重立即就绪
   │      │             （src_confidence = user_explicit / preset，见 [02 §3.2](./02-三层关键词权重模型.md)）
   │      └─ auto  模式：L1 暂为空（无预设场景），待对话中由 LLM 判定（见步骤 3）
   │      生成 Agent 开场称呼（详见 [02 §12](./02-三层关键词权重模型.md)）：
   │          · L1 有权重 → 调用模型依场景权重生成场景感知称呼
   │          · L1 无权重（auto 模式冷启动）→ 回退默认兜底（你好 / hi）
   │      LVM 据当前 L1 权重生成 Agent 初始身份关键字（确定性、不调 LLM）
   │      —— 详见 [06 §3](./06-向量化词库与在线学习.md)
   │
   ├─ 1. 落库（messages 表，role=user）
   │
   ├─ 2. 触发判定 trigger.should_analyze(turn, message, state)
   │      ├─ 否 → 跳过分析，直接复用上一轮权重
   │      └─ 是 ↓
   │
   ├─ 3. 证据抽取
   │      ├─ heuristics.extract()          本地规则，零成本，同步
   │      └─ llm_analyzer.extract()        LLM 抽取，异步，带 JSON 校验
   │           → 合并 + 白名单词表过滤 + 强度裁剪 [0,1]
   │      （auto 模式）llm_analyzer 额外产出 scene_ops → 实时更新 L1 场景关键词
   │           —— 详见 [06 §4.4](./06-向量化词库与在线学习.md)
   │
   ├─ 4. 更新 L2
   │      E_t = Σ intensity_i × exp(-λ(t - t_i)) + E_preset
   │      conf = E_t / (E_t + K)
   │      w2   = base × conf × salience
   │
   ├─ 5. 重算 L3 = f(L1, L2)
   │      规则推导 → 可选 LLM 精炼 → 融合 → 维度归一化
   │
   ├─ 6. 稳定性判定
   │      Δ = 与上一轮 L3 的距离
   │      Δ < τ_rebuild → 沿用旧 Prompt（不重建）
   │      否则重建，Δ ≥ τ_notify 时向用户给出可见提示
   │
   ├─ 7. 组装 Prompt（预算裁剪 + 渲染）
   │      （doc/07 启用时）先经 LangGraph `retrieve_cold` 检索冷记忆 → 作为"背景参考"区块注入，
   │      占独立子预算 DLA_MEMORY__PROMPT_RATIO，不参与权重计算；
   │      **默认历史注入 = 历轮 `turn_summary` 链**（见 [07 §4.6](./07-对话历史冷热记忆(LangGraph+RAG).md)），
   │      完整原文不进默认 Prompt；若上轮 LLM 发起 `recall_memory`，本轮附 `detail_blocks` 原文片段
   │      —— 详见 [07 §3/§4](./07-对话历史冷热记忆(LangGraph+RAG).md)
   │
   ├─ 8. 调用 LLM 流式生成 → 逐 token 回调给交互层
   │      LLM 输出须含 `turn_summary`（≤100 字）与 doc/06 结构化字段；
   │      若模型需调用本地工具 → **经 ToolRouter 两级路由**（低成本路由粗筛 top-N 候选 → LLM 在候选中选定）
   │      → executor 派发至 `ToolRegistry` 执行 → 结果进当前轮上下文 → 重新生成
   │      （现有的 `recall_memory` 成为 `tools/plugins/` 下首个内置插件；受 TOOL_MAX_LOOPS 限制）
   │      —— 详见 [08 §4/§5](./08-工具插件化与路由.md)
   │
   ├─ 9. 落库：messages(role=agent) + weight_snapshots × 3 + persona_changes + turn_summary
   │      （doc/07 启用时）LangGraph `reflect_store` 将本轮切块 embed 写入冷库（含 `turn_summary` 索引字段），
   │      滑出热窗口的老轮次迁入冷库（原文保留供 `recall_memory` 取回）；图状态经 SqliteSaver checkpoint
   │
   ├─ 10. 返回本轮权重摘要（供 UI 渲染 / CLI --explain 打印）
   │
   └─ 11. 在线学习（LVM）
          依据本轮 `agent_ops` 与 `satisfaction` 构造监督信号（q_t → p*）
          对关联矩阵 M_r 做一步解析梯度更新（学习率 η、梯度裁剪、回放缓冲）
          写入 lvm_training_log 与检查点（M_r / 动量 / 步数）
          —— 详见 [06 §6](./06-向量化词库与在线学习.md)
```

## 8. 配置与环境变量

`.env.example` 仅含占位值，真实 `.env` 必须进 `.gitignore`，任何输出中不得出现真实密钥。

```bash
# ---- LLM ----
DLA_LLM__API_KEY=sk-placeholder
DLA_LLM__BASE_URL=https://api.openai.com/v1
DLA_LLM__MODEL=gpt-4o-mini
DLA_LLM__ANALYZER_MODEL=              # 留空则复用 MODEL；分析可用更便宜的模型
DLA_LLM__TIMEOUT_SECONDS=60
DLA_LLM__MAX_RETRIES=2

# ---- 存储 ----
DLA_DB__PATH=./data/dla.db
DLA_DB__AUTO_MIGRATE=true

# ---- 权重引擎 ----
DLA_WEIGHT__PRIOR_STRENGTH=2.0        # K，置信度平滑系数
DLA_WEIGHT__DEFAULT_HALF_LIFE_HOURS=6 # 观察项证据半衰期
DLA_WEIGHT__LLM_FUSION_BETA=0.3       # β，LLM 精炼结果融合权重
DLA_WEIGHT__REBUILD_THRESHOLD=0.15    # τ_rebuild
DLA_WEIGHT__NOTIFY_THRESHOLD=0.40     # τ_notify
DLA_WEIGHT__REBUILD_COOLDOWN_TURNS=2

# ---- Prompt 预算 ----
DLA_PROMPT__TOTAL_TOKEN_BUDGET=900
DLA_PROMPT__L1_RATIO=0.25
DLA_PROMPT__L2_RATIO=0.20
DLA_PROMPT__L3_RATIO=0.55

# ---- 分析触发 ----
DLA_ANALYSIS__COLD_START_TURNS=2
DLA_ANALYSIS__PERIOD=3
DLA_ANALYSIS__ENABLE_HEURISTICS=true

# ---- 本地向量化与在线学习（doc/06）----
DLA_LVM__DIM=64                         # 关键词向量维度 d
DLA_LVM__BACKBONE=                      # 留空=随机初始化(默认)；可选 sentence-transformers 模型名(语义热启动)
DLA_LVM__LEARNING_RATE=0.01             # η0，关联矩阵更新学习率
DLA_LVM__LR_DECAY=0.0                   # 步衰减系数
DLA_LVM__MOMENTUM=0.9                   # 带动量 SGD
DLA_LVM__GRAD_CLIP=1.0                  # 梯度范数裁剪
DLA_LVM__WEIGHT_DECAY=1e-4              # 权重衰减 λ_reg
DLA_LVM__TEMP=0.2                       # 关联→salience 的 softmax 温度 τ
DLA_LVM__L3_PRIOR_GAMMA=0.2             # γ：LVM 学习先验融合权重
DLA_LVM__TOP_T_IDENTITY=6               # 冷启动 Agent 身份关键字数量
DLA_LVM__MARGIN_LAMBDA=0.1             # 排序边界损失权重 λ_rank
DLA_LVM__REPLAY_BUFFER=256             # 回放缓冲容量
DLA_LVM__TRAIN_EVERY_N_TURNS=1         # 每 N 轮做一次更新

# ---- 运行模式 ----
DLA_MODE__SCENARIO=fixed                # fixed | auto；fixed=用户指定场景模板，auto=LLM 实时判定并更新场景关键词
DLA_MODE__AUTO_WARMUP_TURNS=3          # auto 模式：前 N 轮优先建立场景，之后才稳定

# ---- 对话历史冷热记忆（doc/07, LangGraph + RAG）----
DLA_MEMORY__ENABLE=true                    # 是否启用对话历史记忆
DLA_MEMORY__HOT_WINDOW=6                   # 热记忆滑动窗口轮数
DLA_MEMORY__COLD_TOP_K=4                   # 冷记忆检索返回条数
DLA_MEMORY__EMBED_DIM=384                  # 记忆向量维度（sentence-transformers 默认 384）
DLA_MEMORY__EMBED_BACKBONE=                # 留空=确定性占位(检索退化但结构可测)；可选 sentence-transformers 模型名(语义热启动)
DLA_MEMORY__RETRIEVE_TRIGGER=periodic      # always | periodic | similarity | coldstart
DLA_MEMORY__RETRIEVE_PERIOD=3              # periodic 模式触发周期（轮）
DLA_MEMORY__RETRIEVE_SIM_THRESHOLD=0.3    # similarity 模式相似度阈值
DLA_MEMORY__PROMPT_RATIO=0.15             # 冷记忆占 Prompt 预算比例（复用 doc/02 预算纪律）
DLA_MEMORY__COMPACT_EVERY_N_SESSIONS=10    # 跨会话压缩周期
DLA_MEMORY__IMPORTANCE_DECAY_DAYS=30       # 重要性时间衰减半衰期
DLA_MEMORY__CHECKPOINTER=sqlite            # 状态持久化后端（LangGraph）
DLA_MEMORY__INJECT_SUMMARIES_ONLY=true     # 下一轮历史仅注入 turn_summary 链（不注入完整原文）
DLA_MEMORY__SUMMARY_MAX_CHARS=100          # turn_summary 硬上限（字符，中文按字计）
DLA_MEMORY__TOOL_RETRIEVE_TOP_K=3          # recall_memory 每次取回的原文片段数
DLA_MEMORY__TOOL_MAX_LOOPS=2               # 单轮内工具循环上限（防死循环）

# ---- 开场称呼生成 ----
DLA_GREETING__ENABLE=true              # 是否生成场景感知开场称呼（false 则始终用兜底）
DLA_GREETING__MODEL=                   # 留空复用 DLA_LLM__MODEL；可用更便宜的模型生成称呼
DLA_GREETING__FALLBACK_POOL=你好,hi,嗨  # 无场景权重时轮询/随机的默认称呼
DLA_GREETING__REGEN_ON_SCENE_CHANGE=true  # auto 模式下场景关键词显著变化时是否重新生成称呼

# ---- 工具插件与路由（doc/08）----
DLA_TOOLS__ENABLED=true
DLA_TOOLS__PLUGIN_DIR=tools/plugins
DLA_TOOLS__ROUTER=hybrid
DLA_TOOLS__ROUTER_TOP_N=6
DLA_TOOLS__AUTO_RELOAD=off
DLA_TOOLS__HOTRELOAD_SHADOW=true
DLA_TOOLS__MAX_CONCURRENT=4
DLA_TOOLS__FALLBACK_ALL_ON_MISS=true

# ---- 日志 ----
DLA_LOG__LEVEL=INFO
DLA_LOG__JSON=true
```

启动时由 `settings.py` 一次性校验：缺失必填项立即抛 `ConfigError` 并给出可读提示，绝不带着坏配置跑起来。

## 9. 错误体系与降级策略

```python
class DlaError(Exception): ...            # 基类
class ConfigError(DlaError): ...          # 配置缺失/非法
class LexiconError(DlaError): ...         # 词库加载失败
class LlmError(DlaError): ...             # 上游调用失败（含子类）
class LlmRateLimitError(LlmError): ...
class LlmSchemaError(LlmError): ...       # 结构化输出不合规
class AnalysisError(DlaError): ...        # 分析流程异常
class StorageError(DlaError): ...
```

降级矩阵：

| 故障 | 处理 | 用户可见性 |
|---|---|---|
| 配置缺失 | 启动即失败，打印缺哪个变量 | 明确的启动错误 |
| 词库 YAML 非法 | 启动即失败，指出文件与行号 | 明确的启动错误 |
| LLM 分析超时/失败 | 重试 1 次后跳过本轮分析，沿用旧权重 | 无感（`--explain` 下可见） |
| LLM 输出非法 JSON | 尝试括号提取修复 → 失败则丢弃该批证据 | 无感 |
| 抽取到词表外关键词 | 丢弃 + 记 warn 日志（用于反哺词库） | 无感 |
| 主对话生成失败 | 重试 2 次（指数退避）→ 返回友好错误，不抛栈 | 明确的错误提示 |
| SQLite 写入失败 | 事务回滚，返回错误；对话本身不中断 | 提示"本轮未能保存" |

**通用原则**：客户端永远看不到堆栈、密钥、内部路径；日志不记录消息正文（除非显式开启 `DLA_LOG__LOG_CONTENT=true` 用于调试）。

## 10. 风险与权衡

| 风险 | 影响 | 缓解措施 |
|---|---|---|
| LLM 抽取的关键词不稳定，同类表述给出不同词 | 权重抖动 | 白名单词表 + 同义词归一 + 半衰期平滑 + 冷却轮次 |
| 长对话后证据堆积，老印象洗不掉 | 用户画像僵化 | 指数衰减 + 提供 `dla portrait reset --soft` 软重置 |
| Prompt 过长导致成本上升 | 费用与延迟 | 分层预算硬裁剪 + 每轮快照监控 token 数 |
| 规则与 LLM 精炼结论打架 | 人格不一致 | β 融合 + 分歧超阈值时以规则为准并记日志 |
| PyQt 许可证合规 | 法律风险 | `qt_compat` 抽象层，可低成本切 PySide6 |
| 权重参数凭感觉调，缺乏依据 | 效果调优困难 | `dla bench` 剧本回放 + 快照数据离线分析 |
| LangGraph 成为额外强依赖 | 依赖体积与学习成本上升 | 纯 Python、离线可装；checkpointer 无运行时联网；核心引擎不反向依赖它（边界用测试守住） |
| 冷记忆检索质量差（占位 embedding） | 长期记忆形同虚设 | 默认占位仅用于架构验证；真实语义需配置 `DLA_MEMORY__EMBED_BACKBONE`（首次下载后离线） |
| 冷记忆挤占上下文 / 劫持人格 | 成本上升或行为偏移 | 严格 `PROMPT_RATIO` 上限 + 相似度/importance 过滤；冷记忆仅作"背景参考"注入、不参与权重 |
| 工具插件热更新导致进行中对话不一致 | 行为错乱 | 原子快照 + 版本化；进行中对话持旧快照引用，加载失败自动回滚 last-good |
| 路由漏选 / 全量 schema 注入 | 该用没用上 / token 膨胀 | `FALLBACK_ALL_ON_MISS` + 漏选告警；路由 top-N 控制注入规模 |

---

下一步：阅读 [02 · 三层关键词权重模型](./02-三层关键词权重模型.md)，这是整个项目的技术核心。
